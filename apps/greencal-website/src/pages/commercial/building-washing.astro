---
import ServicePageLayout from '../../layouts/ServicePageLayout.astro';
import { services } from '../../data/services';

const service = services.find((s) => s.slug === 'building-washing')!;
---

<ServicePageLayout service={service} />
