# Root environment variables
NODE_ENV=development
API_GATEWAY_PORT=4000
CORE_API_PORT=4001
AGENT_ORCHESTRATOR_PORT=4002
WORKER_SERVICE_PORT=4003
WEB_CONSOLE_PORT=3000
ADMIN_CONSOLE_PORT=3001
DOCS_PORTAL_PORT=3002

# Example service variables
LOG_LEVEL=debug

# --- Cloud infrastructure preparation (DECISIONS.md ADR-0008) ---
# Placeholder names only — no real keys/values. Do not fill these in
# until a provider is actually, explicitly authorized for connection.
# See docs/cloud/AI_PROVIDER_INTEGRATION.md.
AI_GATEWAY_PORT=4100
JERVIS_API_PORT=4101

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
ZAI_GLM_API_KEY=
DEEPSEEK_API_KEY=
PERPLEXITY_API_KEY=
GEMINI_API_KEY=
KIMI_CLI_TOKEN=

# Per docs/cloud/COST_CONTROL_POLICY.md — enforced by
# packages/cost-controller once real spend exists.
DAILY_SPEND_LIMIT_USD=25
MONTHLY_SPEND_LIMIT_USD=400

# Hostinger VPS / Docker Compose cloud stack (infra/docker/) — not
# provisioned yet. See docs/cloud/HOSTINGER_VPS_SETUP.md.
REDIS_URL=
POSTGRES_URL=
N8N_ENCRYPTION_KEY=
